# Entwicklungsumgebung des Projekts.
#
#   conda env create -f environment.yml
#   conda activate online-judge
#
name: online-judge

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
