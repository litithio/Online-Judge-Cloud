# Werkzeuge für Lint und Infra-Checks. Einzige Quelle der Versionen: die CI
# installiert genau diese Datei. Einrichten:
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Vorausgesetzt wird Python 3.12, dieselbe Version wie in den Workflows.
# venv erzwingt sie nicht, es übernimmt das python3, das beim Anlegen der
# Umgebung in der PATH steht: notfalls python3.12 -m venv .venv aufrufen.
#
# Eine systemweit installierte Version kann die der Projektumgebung verdecken;
# scripts/infra-check.sh prüft die Übereinstimmung und warnt bei Abweichung.

ruff==0.15.22
ansible-lint==26.6.0
ansible-core==2.21.2
# Läuft nicht in dieser venv, sondern im Worker-Image. scripts/unit-tests.sh
# liest die Version von hier und installiert sie in den Wegwerf-Container,
# damit die Pins an einer Stelle stehen.
pytest==9.1.1
